프로젝트 개발 지침

이 문서는 Claude Code가 이 프로젝트에서 코드를 작성/수정할 때 항상 지켜야 하는 규칙입니다.

1. 데이터 페칭 — React Query
서버 상태(API 응답, DB 데이터)는 반드시 **React Query(TanStack Query)**로 관리한다. useEffect + useState로 직접 fetch하지 않는다.
Query key는 배열 형태로 명확히 정의하고, 관련 도메인별로 queryKeys.ts 등에 모아서 관리한다.
예: ['users', userId], ['posts', { page, filter }]
staleTime, gcTime을 데이터 성격에 맞게 명시적으로 설정한다. 자주 안 바뀌는 데이터는 staleTime을 길게 잡는다.
mutation 이후에는 invalidateQueries 또는 setQueryData로 캐시를 갱신한다. 화면 새로고침으로 데이터를 다시 받아오게 하지 않는다.
에러/로딩 상태는 각 컴포넌트에서 개별 처리하지 말고, 공통 ErrorBoundary + Suspense 또는 공통 로딩/에러 UI 패턴을 따른다.
2. 컴포넌트 구조화
컴포넌트는 역할 단위로 분리한다: components/ui(순수 프레젠테이션), components/features/{도메인}(비즈니스 로직 포함), hooks/(커스텀 훅), lib/ 또는 utils/(순수 함수).
하나의 컴포넌트 파일이 200줄을 넘어가면 하위 컴포넌트나 훅으로 분리를 고려한다.
Props는 최소한으로: 컴포넌트가 필요 이상의 데이터를 받지 않도록 하고, 객체 전체가 아니라 필요한 필드만 내려준다.
비즈니스 로직(API 호출, 상태 계산 등)은 컴포넌트 안에 두지 않고 커스텀 훅으로 분리한다. 컴포넌트는 UI 렌더링에만 집중한다.
조건부 렌더링이 3단계 이상 중첩되면 별도 컴포넌트로 추출한다.
3. 보안 — 프론트에서 막은 건 백엔드에서도 반드시 막는다
프론트엔드의 검증/권한 체크는 UX 개선용일 뿐, 보안 수단이 아니다. 프론트에서 버튼을 숨기거나 입력을 막았다고 해서 백엔드 검증을 생략하지 않는다.
다음 항목은 프론트/백엔드 양쪽에서 이중으로 검증한다:
입력값 유효성 검사 (길이, 타입, 형식 등) — 프론트는 UX용, 백엔드는 필수
인증(로그인 여부) / 인가(권한, 소유권 체크) — 반드시 API 레벨(미들웨어, 라우트 핸들러)에서 재확인
가격, 수량, 할인율 등 금액과 관련된 값은 클라이언트에서 보낸 값을 절대 신뢰하지 않는다. 서버에서 재계산하거나 DB 기준으로 검증한다.
API 응답에는 필요한 필드만 내려준다. 내부용 필드(비밀번호 해시, 관리자 메모 등)를 실수로 노출하지 않았는지 항상 확인한다.
사용자 입력은 서버 단에서 sanitize/validate 후 저장한다 (SQL Injection, XSS 방지).
4. 렌더링 최적화 — useCallback / useMemo / React.memo
무조건 다 감싸지 않는다. 아래 기준에 해당할 때만 적용한다:
useCallback: 자식 컴포넌트(특히 React.memo로 감싼)에 콜백을 props로 내려줄 때, 또는 useEffect의 의존성 배열에 함수가 들어갈 때
useMemo: 계산 비용이 실제로 큰 연산(정렬, 필터링, 집계 등)이거나, 참조 동일성이 중요한 객체/배열을 자식에게 내려줄 때
React.memo: 렌더링 비용이 크고, 부모가 자주 리렌더링되는데 props는 자주 안 바뀌는 컴포넌트
단순한 값 계산이나 렌더링 비용이 낮은 컴포넌트에는 남용하지 않는다 — 오히려 메모이제이션 자체의 오버헤드가 더 클 수 있다.
리스트 렌더링 시 key는 index가 아니라 고유 id를 사용한다.
무거운 리스트는 가상화(virtualization, 예: react-virtual, react-window)를 고려한다.
5. 상태 관리 — Zustand
서버 상태는 Zustand에 두지 않는다 (그건 React Query의 역할).
Zustand는 다음과 같은 클라이언트 전역 상태에만 사용한다:
여러 페이지/컴포넌트에서 공유되는 UI 상태 (예: 사이드바 열림/닫힘, 모달 상태, 테마)
인증 토큰, 로그인한 유저 정보 같은 세션성 클라이언트 상태
폼이 여러 단계(멀티스텝)에 걸쳐 있고 상태를 유지해야 할 때
컴포넌트 로컬에서만 쓰이는 상태는 Zustand로 올리지 말고 useState로 충분하다. props 한두 단계로 전달 가능한 상태를 굳이 전역화하지 않는다.
스토어는 도메인별로 분리한다 (useAuthStore, useUIStore 등 하나의 거대한 스토어에 다 넣지 않는다).
selector를 사용해 필요한 필드만 구독한다 (useStore(state => state.user)), 스토어 전체를 구독해서 불필요한 리렌더링을 유발하지 않는다.
6. Vercel 스킬 자동 적용
React 코드 작성/수정 시 다음 Vercel 스킬을 항상 적용한다:
vercel-react-best-practices: 모든 React/Next.js 코드는 이 스킬의 가이드라인 준수 (데이터 페칭 최적화, 워터폴 제거, 번들 크기, 렌더링 최적화 등)
vercel-composition-patterns: 컴포넌트 설계 시 boolean prop 피하기, compound components, 상태 끌어올리기 등 적용
성능 최적화 시 즉시 적용하며, 코드 리뷰/리팩토링 시 이 지침들이 위반되었는지 검토한다.

작업 시 체크리스트

코드를 작성/수정한 뒤 아래를 스스로 점검한다:

 서버 데이터를 React Query 없이 직접 fetch하지 않았는가
 컴포넌트가 너무 커지거나 로직이 섞이지 않았는가
 프론트에서 막은 권한/검증을 백엔드에서도 동일하게 막았는가
 useCallback/useMemo/React.memo를 근거 없이 남발하지 않았는가
 전역이 아니어도 되는 상태를 Zustand에 넣지 않았는가
 Vercel 베스트 프랙티스 가이드라인을 준수했는가 (워터폴, 번들, 렌더링 최적화)
 컴포넌트 구조가 composition patterns를 따르는가 (boolean prop 최소화, 명확한 인터페이스)